<%= form_for(hungry_person) do |f| %>
  <% if hungry_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hungry_person.errors.count, "error") %> prohibited this hungry_person from being saved:</h2>

      <ul>
      <% hungry_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
